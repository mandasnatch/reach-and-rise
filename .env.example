# Optional connected DEMO workspace. Without these, the browser-local demo works.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Generate a random secret of at least 32 characters. Codes must be at least 12.
AUTH_SECRET=
PATIENT_ACCESS_CODE=
THERAPIST_ACCESS_CODE=
# Optional email notification. Configure server-side in Vercel; never expose keys in browser code.
RESEND_API_KEY=
EMAIL_FROM=
THERAPIST_EMAIL=
APP_URL=
